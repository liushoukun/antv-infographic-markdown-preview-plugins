# 变更日志

本文件随 VSIX 一并发布；完整 monorepo 说明见 GitHub 仓库根目录 README。

## 1.0.0

- 正式发布：与 JetBrains 插件版本对齐为 1.0.0。

## 0.8.0

- 初始以 monorepo 结构发布：共享预览脚本位于 `packages/preview-web`，扩展位于 `apps/vscode`。
- Markdown 中 `infographic` 围栏预览、`.infographic` 文件、侧栏可视化编辑等能力（与历史版本一致，详见市场页说明）。
